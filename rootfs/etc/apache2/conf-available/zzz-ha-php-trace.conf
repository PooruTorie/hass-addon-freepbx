# Extra tracing for PHP requests in Apache (mod_php)
# This is safe-ish but adds verbosity.

<IfModule mod_php.c>
  LogLevel php:trace3
</IfModule>

# If mod_security or other modules interfere, their errors should now show.

