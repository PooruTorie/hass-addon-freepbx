; Force PHP error logging into a real file under /data so we can tail it from s6.
; /proc/self/fd/2 may not always be consumed by the supervisor depending on SAPI/error handler.
log_errors=On
error_log=/data/php-error.log
error_reporting=E_ALL
display_errors=Off
display_startup_errors=On
